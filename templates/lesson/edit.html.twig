{% extends 'base.html.twig' %}

{% block title %}Редактирование {{ lesson.name }} / StudyOn{% endblock %}

{% block body %}
    <h1>{{ lesson.name }}</h1>
    <p>
        Курс
        <a href="{{ path('course_show', {'id': lesson.getCourse.id}) }}">{{ lesson.getCourse.name }}</a>
    </p>


    {{ include('lesson/_form.html.twig', {'button_label': 'Сохранить'}) }}

    {{ include('lesson/_delete_form.html.twig') }}
{% endblock %}
