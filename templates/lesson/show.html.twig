{% extends 'base.html.twig' %}

{% block title %}{{ lesson.Name }} / {{ lesson.getCourseID.Name }} / StudyOn{% endblock %}

{% block body %}
    <h1>{{ lesson.Name }}</h1>

                <h2>
                    <a href="{{ path('course_show', {'id': lesson.getCourseID.id}) }}">{{ lesson.getCourseID.Name }}</a>
                </h2>

                <a href="{{ path('lesson_edit', {'id': lesson.id}) }}">edit</a>

                <h3>Содержимое</h3>
                {{ lesson.Content }}

    {{ include('lesson/_delete_form.html.twig') }}
{% endblock %}
