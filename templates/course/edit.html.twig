{% extends 'base.html.twig' %}

{% block title %}Редактирование {{ course.Name }} / StudyOn{% endblock %}

{% block body %}
    <h1>Редактирование {{ course.Name }}</h1>

    {{ include('course/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('course_index') }}">Вернуться к списку</a>

    {{ include('course/_delete_form.html.twig') }}
{% endblock %}
