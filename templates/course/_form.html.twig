{% form_theme form 'form_errors.html.twig' %}
{{ form_start(form) }}
    {{ form_widget(form) }}
<div class="row">
    <button class="btn btn-outline-primary m-sm-1 btn-sm">{{ button_label|default('Сохранить') }}</button>
{{ form_end(form) }}